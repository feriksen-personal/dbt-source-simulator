# ============================================================================
# Environment Variables for dbt-azure-demo-source-ops
# ============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env to git - it's in .gitignore

# ----------------------------------------------------------------------------
# Azure SQL Configuration (Optional - only needed for Azure SQL demos)
# ----------------------------------------------------------------------------
# Get these from your Azure SQL database provisioned via:
# https://github.com/feriksen-personal/dbt-azure-demo-source-data

DEMO_SQL_SERVER=your-server-name
DEMO_SQL_USER=sqladmin
DEMO_SQL_PASSWORD=your-secure-password

# ----------------------------------------------------------------------------
# MotherDuck Configuration (Optional - only needed for MotherDuck demos)
# ----------------------------------------------------------------------------
# Get your token from: https://motherduck.com/
# After signing up (email/password recommended):
# 1. Log in to MotherDuck web console
# 2. Go to Settings → Service Tokens
# 3. Click "Create Token"
# 4. Copy the token and paste below

MOTHERDUCK_TOKEN=your-motherduck-service-token

# ----------------------------------------------------------------------------
# Notes:
# ----------------------------------------------------------------------------
# - DuckDB (local) requires NO environment variables
# - Azure SQL requires DEMO_SQL_* variables
# - MotherDuck requires MOTHERDUCK_TOKEN only
# - You only need to set variables for the platform(s) you're using
